import {resetTestEnv} from "../../../utils/resetTestEnv";

describe('Spawn event', () => {
    beforeEach(async () => {
        await resetTestEnv();
    });

    afterAll(async () => {
    });

    test('spawn', async () => {
        // given

        // when

        // then

    });

});